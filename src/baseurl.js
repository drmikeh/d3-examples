// GitHub Pages will use a baseUrl of `drmikeh.github.com/d3-examples` so we
// need to set the basename to include the `/d3-examples` prefix.
// Otherwise we use an empty string for the basename.
const baseUrl = window.location.hostname.includes('github') ? '/d3-examples' : '';
